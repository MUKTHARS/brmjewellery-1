// Delegates to [id] page with id='new' — Next.js static route takes priority
// This file just renders the create form directly
import CategoryFormPage from '../[id]/page';
export default CategoryFormPage;
